select s.* from subject s join mark m2 on s.id = m2.subject_id group by m2.student_id having avg(mark) > (select avg(mark) from mark m);
select s.* from student s join payment p on s.id = p.student_id group by p.student_id having avg(amount) < (select avg(amount) from payment);